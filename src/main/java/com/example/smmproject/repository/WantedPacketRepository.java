package com.example.smmproject.repository;

import com.example.smmproject.entity.WantedPacket;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WantedPacketRepository extends JpaRepository<WantedPacket,Long> {
}
